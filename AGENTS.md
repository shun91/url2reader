# AGENTS.md

## docs/adr 運用ルール

### 目的

- 重要な設計判断を「なぜ」を含めて残し、将来の変更で再利用できる状態を保つ。

### フォーマット

- 配置: `docs/adr/`
- 項目: 既存のadrを確認して把握する

### 記述方針

- ADR追加・更新時は、対応実装との差分有無を確認する。
- 実装がADRから逸脱する場合、実装だけ先行させずADR更新方針を先に決める。

### 運用フロー

- 変更要求時にADR対象判定。
- 対象なら既存ADRを確認し、重複回避のうえ新規/改訂方針を決定。
- 実装とADRを同一変更セットでレビュー可能にし、マージ前に `Status` と `Related Files` を最終確認。
