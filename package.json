{
  "name": "url2reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "test": "vitest"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "linkedom": "^0.18.12"
  },
  "devDependencies": {
    "vitest": "^2.1.8",
    "wrangler": "^4.36.0"
  }
}
